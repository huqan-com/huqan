# AXIOM v0.5 Backlog

## Scope Lock

- [ ] Keep the branch focused on Agent OS contract work only.
- [ ] Do not implement discovery engine yet.
- [ ] Do not change runtime behavior until the workflow-agent contract is approved.
- [ ] Treat `repo-memory` and `company-brain` as Agent OS tools, not standalone phases.

## Task 1 - Workflow Agent Contract

Files:
- Create: `workflow-agent.js`
- Create: `workflow-agent.test.js`

Goal:
Define the new agent entrypoint and its report shape.

Acceptance:
- `plan(goal, opts)` returns a structured plan.
- `run(goal, opts)` returns a structured execution report.
- The report includes `report`, `nextAction`, `recommendations`, and `finalAnswer`.
- Legacy envelope behavior stays additive.

## Task 2 - Tool Registry Contract

Files:
- Create: `toolRegistry.js`
- Create: `toolRegistry.test.js`

Goal:
Centralize tool discovery, policy checks, and invocation routing.

Acceptance:
- The registry exposes `registerTool()`, `listTools()`, `getTool(name)`, and `runTool(name, input, context)`.
- The registry consults `toolPolicy.js` before execution.
- Tool metadata is returned in a testable structure.

## Task 3 - Tool Adapters

Files:
- Modify: `plugins/repo-memory.js`
- Modify: `plugins/company-brain.js`
- Modify: `plugin.js` only if the shared capability contract needs a small helper

Goal:
Make the existing company tools behave like first-class Agent OS tools.

Acceptance:
- `repo-memory` is callable from the registry.
- `company-brain` is callable from the registry.
- Tool results carry enough metadata for the agent report.
- No discovery engine code lands here.

## Task 4 - Surface Wiring

Files:
- Modify: `cli.js`
- Modify: `server.js`
- Modify: `mcpServer.js`

Goal:
Expose the same workflow-agent behavior through every surface.

Acceptance:
- CLI shows the new report fields.
- REST returns the same structured output.
- MCP exposes the same data without a second contract.
- Legacy flows keep working.

## Task 5 - Regression

Files:
- Modify: `cli.test.js`
- Modify: `mcpServer.test.js`
- Modify: `server.test.js`
- Create: `workflow-agent.test.js`
- Create: `toolRegistry.test.js`

Goal:
Lock the new contract with tests before the implementation grows.

Acceptance:
- Existing test suites stay green.
- New tests cover tool registration, routing, and report shape.
- `repo-memory` and `company-brain` continue to work under the new agent contract.

## v0.5 Milestones

1. Contract lock
2. Tool wiring
3. Surface exposure
4. Regression lock

## Not In This Slice

- Discovery engine implementation
- Obsidian polish
- Slack/Gmail/Jira/Linear/Notion connectors
- Multi-user permissions
- Governance layer expansion
