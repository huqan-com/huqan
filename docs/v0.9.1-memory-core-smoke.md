# AXIOM v0.9.1 Memory Core Smoke Checklist

Use this checklist before sealing or releasing the Memory Core line.

Release prep state: `603 pass / 0 fail / 16 skipped` with deterministic memory graph link ordering merged.

## Required Smoke Commands

```bash
node --test test/memory-schema.test.js test/kernel-memory-api.test.js test/memory-persistence.test.js test/memory-query-helpers.test.js test/memory-graph-links.test.js test/memory-temporal-query.test.js test/memory-provenance-audit.test.js test/memory-workspace-isolation.test.js
npm test
git diff --check
```

## Pass Criteria

- All targeted Memory Core tests pass.
- Full `npm test` passes.
- `git diff --check` is clean.
- No runtime artifacts are staged.
- No package metadata drift is introduced.
- No unrelated files are touched.

## Smoke Checklist

### Schema and API

- `memory-schema.test.js`
- `kernel-memory-api.test.js`

### Persistence

- `memory-persistence.test.js`

### Query Helpers

- `memory-query-helpers.test.js`

### Graph Links

- `memory-graph-links.test.js`

### Temporal Queries

- `memory-temporal-query.test.js`

### Provenance and Audit

- `memory-provenance-audit.test.js`

### Workspace Isolation

- `memory-workspace-isolation.test.js`

## Developer Notes

- Memory content stays immutable.
- Duplicate content in the same workspace is idempotent.
- Deletion is a tombstone, not a physical delete.
- `supersede` creates a new memory plus a relation.
- `contradict` creates a new relation.
- Queries must stay workspace-scoped and deterministically sorted.
- Persistence reload must preserve provenance, audit, and workspace boundaries.

## Expected Final State

- Branch is ready for review.
- Docs-only diff is intentional.
- No merge is performed by this PR.
